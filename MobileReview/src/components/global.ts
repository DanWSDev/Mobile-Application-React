export let gameVar: number[] = [0,0,0,0];
export let gmimage: string[] = [];
export let userRole: string[] = ["normal"];
/*export let revs = 0;
export  gamid:number = '';
export let avgrev = 0;

export function updateGlobalVariables(revs: number, avgrev: number, gamid: number) {
  revs = revs;
  avgrev = avgrev;
  gamid = gamid;
}*/